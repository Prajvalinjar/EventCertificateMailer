{
  "name": "event-certificate-mailer",
  "version": "1.0.0",
  "description": "EventCertificateMailer - Personalized event certificate generation and bulk email dispatch via Gmail SMTP",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build step required for static assets'"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "nodemailer": "^9.1.1",
    "resend": "^6.26.0"
  }
}
